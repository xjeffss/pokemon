<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
        <h1>Welcome <%=players.name%></h1>
        
    <form action='/players/profile/<%=players.id%>?_method=PUT' method='POST'>
        Name: <input type='text' name='name' value='<%=players.name%>'/><br/>
		<!--  NOTE: the form is pre-populated with values for the server-->
		Username: <input type='text' name='username' value='<%=players.username%>'/><br/>
		Password: <input type='text' name='password' value='<%=players.password%>'/><br/>
         <br/>
        Team: <select name="teamId">
                <% for (let i = 0; i<teams.length; i++) {%>
                    <option value = "" disabled selected hidden <%=players.Team.name%></option>
                    <option value="<%=teams[i].id%>" selected=<%=teams[i].id%>><%=teams[i].name%>
                    </option>
            <% } %>
        </select>
        <br/>
        Pokemon: <select name="pokemon"
        <% for (let i = 0; i<pokemon.length; i++) {%>
            <option value="<%=pokemon[i].id%>" selected=<%=pokemon[i].id%>><%=pokemon[i].name%>
            </option>
            <% } %>
         <input type='submit' value='Edit'/>
    </form>

        <form action="/players/profile/<%=players.id%>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
        </form>
<h2><%=players.name%> owns these Pokemon:</h2>
  <% for (let i = 0; i<players.Pokemons.length; i++) {%>

    <li>
            <a href="/pokemon/<%=players.Pokemons[i].id%>"><%=players.Pokemons[i].name%></a>
    </li>
    <% } %>
    <br/>
    <br/>

    <a href="/pokemon"> View Pokemon</a>

</body>
</html>